{
  "private": true,
  "scripts": {
    "clean": "rimraf output && rimraf .pulp-cache",
    "build": "pulp build",
    "test": "pulp test"
  },
  "devDependencies": {
    "pulp": "^10.0.4",
    "purescript-psa": "^0.4.0",
    "rimraf": "^2.6.1"
  }
}
